# iosMain — iOS Platform Bindings

This directory is a placeholder for future iOS-specific implementations.

No functional code exists here yet (per T192 / NFR-ARCH-050).

When iOS support is implemented (T190+), add `actual` implementations
of any expect declarations from commonMain here.
