# iosMain — iOS Platform Placeholder

This directory is scaffolded per T192 / NFR-ARCH-050. No functional code exists here.

When iOS support is implemented (T190+), add `actual` implementations of any
`expect` declarations from `commonMain` in this directory.

